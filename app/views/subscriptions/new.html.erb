<div id="content-win-centr" role="main">
  <div id="subscription-content">
  <span class="big-text">Add an e.mote<sup><small>TM</small></sup> (or five...)</span>
    <br/><br/>
    <span class="medium-text-subsc">Each e.mote<sup><small>TM</small></sup> includes:</span>
    <ul class="subsc-list">
      <li>One e.mote<sup><small>TM</small></sup> survey and its Scorecard</li>
      <li>Unlimited e.mote responses</li>
      <li>Replace your e.mote with a different one as many times as you want</li>
      <li>Unlimited sharing and printing of Scorecard results</li>
    </ul>
    
    <% Subscription::OPTIONS.each_with_index do |subscription, i| %>
    <div class="<%= %w(one-pack five-pack ten-pack twentyfive-pack)[i] %>">
      <p class="pack-price"><%= subscription[:price] %> <%= Country.curr_code_to_symbol(Country.find_by_country_code(current_user.country_code || 'US')[:currency]) %> / year</p>
      <span class="pack-purchase"><%= link_to image_tag("buy_button.png"), account_subscriptions_path(:prod_id => subscription[:prod_code]), { :method => :post } %></span>
    </div>
    <% end %>
  </div>
  <span class="subscr_order">For Purchase Order accounts, educational or non-profit discounts, <a href="mailto:sales@inspirationegine.com">please contact us</a>.<br /><br />
    <a href="#">Subscription Terms</a>
  </span>
</div>