<!DOCTYPE html>

<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ --> 
<!--[if lt IE 7 ]> <html lang="en" class="no-js ie6"> <![endif]-->
<!--[if IE 7 ]>    <html lang="en" class="no-js ie7"> <![endif]-->
<!--[if IE 8 ]>    <html lang="en" class="no-js ie8"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en" class="no-js"> <!--<![endif]-->
<head>
  <meta charset="utf-8"/>

  <!-- Always force latest IE rendering engine (even in intranet) & Chrome Frame 
       Remove this if you use the .htaccess -->
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"/>

  <title><%= @title || 'eMote Homie Portal' %></title>
  <meta name="description" content=""/>
  <meta name="author" content=""/>


  <link rel="Shortcut Icon" href="/favicon.ico"/>
  <link href='http://fonts.googleapis.com/css?family=Droid+Sans' rel='stylesheet' type='text/css'/>

  <!-- CSS : implied media="all" -->
  <%= stylesheet_link_tag 'style.css?v=2', :media => :all %>
  <%= csrf_meta_tag %>

  <!-- All JavaScript at the bottom, except for Modernizr which enables HTML5 elements & feature detects -->
  <%= javascript_include_tag 'modernizr-1.6.min.js'  %>

</head>

<body>

  <div id="container">

    <div id="before-header">&nbsp;</div>

        <div id="header">
	    <img id="logo" src="/images/emote_logo.png"/>
			<% if user_signed_in? %>
	    	<div id="menu">
				<div id="username">Welcome, <%= current_user.full_name.empty? ? 'dear user' : current_user.full_name %>!</div>
				<div id="buttons">

		    		<div class="dropdown">
						<%= link_to raw("<div class=\"button #{(controller.class.name == 'SurveysController' && controller.action_name == 'index') ? 'on' : 'off'}\" id=\"emoteControlButton\" style=\"z-index: 10;position: relative;\"></div>"), root_path %>
		    		</div>

		    		<div class="dropdown">
						<a href="#"><div class="button <%= (controller.class.name == 'SurveysController' && controller.action_name == 'scorecard') ? 'on' : 'off' -%>" id="scoreCardButton" style="z-index: 10;position: relative;"></div></a>
            	    	<ul class="menu-body">
							<% current_user.surveys.each do |survey| %>
								<li>
									<%= link_to survey.project_name, scorecard_account_survey_path(survey) %>
								</li>
							<% end %>
        				</ul>
		    		</div>

		    		<div class="dropdown">
						<a href="#"><div class="button <%= (controller.class.name == 'AccountsController' || controller.class.name == 'SubscriptionsController') ? 'on' : 'off' -%>" id="myAccount" style="z-index: 10;position: relative;"></div></a>
            	    	<ul class="menu-body">
        		    		<li>
								<%= link_to 'CHANGE INFORMATION', edit_account_path %>
							</li>
        		    		<li>
								<%= link_to 'VIEW ACTIVITY', account_subscriptions_path %>
							</li>
        		    		<li>
								<%= link_to 'SUBSCRIBE', new_account_subscription_path %>
							</li>
        				</ul>
						<div class="arrow"><img src="/images/buttons/econtrol_arrow.png"/></div>
		    		</div>

				</div>
	    	</div>
			<% end %>
	
	</div>

      <%= yield %>

    <div id="forfooter"> &nbsp; </div>
  </div>

  <div id="footer">
      <img id="ie-logo" src="/images/ie_logo.png"/>
  </div>

  <!-- Javascript at the bottom for fast page loading -->

  <!-- Grab Google CDN's jQuery. fall back to local if necessary -->
  <%= javascript_include_tag :defaults  %>
  <!--[if lt IE 7 ]>
	<%= javascript_include_tag 'dd_belatedpng.js'  %>
    <script>DD_belatedPNG.fix('img, .png_bg'); // Fix any <img> or .png_bg bg-images. Also, please read goo.gl/mZiyb </script>
  <![endif]-->
  <script language="javascript" type="text/javascript">
/*
    if ( ! Modernizr.borderradius || ! Modernizr.boxshadow) {
	var myBorder = RUZEE.ShadedBorder.create({ corner:10, shadow:20 });
	myBorder.render('header');
    }
*/
  </script>
</body>
</html>