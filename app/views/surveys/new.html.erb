<h3>New emote</h3>
<% if @survey.errors.any? %>  
  <div id="errorExplanation">  
    <h2><%= pluralize(@survey.errors.count, "error") %> prohibited this e.mote from being saved:</h2>  
    <ul>  
    <% @survey.errors.full_messages.each do |msg| %>  
      <li><%= msg %></li>  
    <% end %>  
    </ul>  
  </div>  
<% end %>
<% form_for :survey, @survey, :url => account_surveys_path, :html => {:method => :post} do |f| %>
	Enter e.mote subject:<%= f.text_field :project_name %><br/>
	<%= submit_tag 'Publish' %> <%= link_to 'Cancel', account_surveys_path %>
<% end %>